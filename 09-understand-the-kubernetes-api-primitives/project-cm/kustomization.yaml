configMapGenerator:
- name: example
  literals:
  - a=apple
  - b=banana
nameSuffix: "-project"
commonLabels:
  app.kubernetes.io/name: project
  app.kubernetes.io/instance: project
  app.kubernetes.io/version: 0.1.0

